{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "outputMode": "new-only"
    },
    "schema": {
      "outputs": ["options*"],
      "outputMode": "new-only",
      "inputs": ["core/**/typed.ts"]
    },
    "test": {}
  }
}
