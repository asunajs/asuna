---
title: 天翼云盘
description: 天翼云盘使用文档
sidebar:
  order: 4
---

:::caution[特别说明]
出现验证码错误时请考虑在浏览器中登录一下，然后重新尝试，不行就算了
:::

## Node.js 依赖名

[`@asunajs/ecloud`](https://www.npmjs.com/package/@asunajs/ecloud)

## WPS

:::danger
使用 WPS 的用户需要注意！！！

值得注意的是，为了更简单的实现登录，用户手机和密码将上传到 `https://as.js.cool/api/ecloud/rsa` 进行加密，该程序在本项目中开源，请自行查看。

但是由于提供服务的 vercel 在本人手中，本人承诺**仅使用该开源的代码（保证隐私性，数据不会被记录，无人能查看）**。即使这样，我还是能通过更改代码达到记录的效果（因为 vercel 并不一定需要通过 Github 部署，而除我之外没人能看 vercel 的部署日志，所以你并不知道我是否动了手脚），这是一个假设，但存在发生的可能性，即使我承诺。

在使用前你应该清楚这一点，如果你不理解，请不要使用。
:::

### 表名

- `ecloud`
- `天翼云盘`

### 配置

A 列为 手机号

B 列为 密码

使用参考 [使用例子](/demo/wps/)

## 使用方法

:::tip[提示]
文档在建设中，精力有限，欢迎 PR
:::

安装

```bash
npm install @asunajs/ecloud
```

**注意：** `run` 可选参数，配置文件路径

使用 cjs

```js
const { run } = require('@asunajs/ecloud');

(async () => {
  await run();
})();
```

使用 esm

```js
import { run } from '@asunajs/ecloud';

await run();
```

在阿里云函数中使用，其他环境请自行摸索

```js
const { run } = require('@asunajs/ecloud');

exports.handler = async function (event, context, callback) {
  callback(null, await run());
};
```

### 青龙面板

[青龙面板](/guides/qinglong/)

## 配置

:::note[提示]
以下部分内容为自动生成，可能存在部分不准确，仅供参考
:::

@include: ../core/ecloud/options.md

## 手机/账号加密

:::note[提示]
仅 WPS 运行时需要，配置时填写加密后的内容。

输入手机号，鼠标移出输入框后自动加密，点击后面的复制按钮复制加密后的手机号

密码同理
:::

import { RsaEncrypt } from '../../../components/solid/RsaEncrypt.tsx';

<RsaEncrypt name="haha" client:only="solid-js"></RsaEncrypt>
